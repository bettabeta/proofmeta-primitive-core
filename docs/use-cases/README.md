# Domain Profiles

ProofMeta core defines generic protocol primitives. Domain-specific mappings,
vocabularies, and operating guidance belong in separate repositories named
`proofmeta-profile-*`.

A profile can reuse core artifacts without changing the core protocol. This
directory intentionally does not list or endorse any particular profile.
